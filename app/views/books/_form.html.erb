<%= simple_form_for @book do |f| %>
    <%= f.input  :title, label: "Book Title" %>
    <%= f.input  :description %>
    <%= f.input  :author %>
    <%= f.button :submit %>
<% end %>

<!--Creating a form for new users to create a book !--> 